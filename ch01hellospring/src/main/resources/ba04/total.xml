<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">
    <!--主配置文件：包含其他的配置文件，主配置文件一般是不定义对象的
              语法：<import resource="其他配置文件的路径"/>
             关键字：“calsspath”表示路径，（class文件所在的目录）
                    在spring的配置文件中要制定其他文件的位置，需要使用classpath告诉spring去哪里加载读取文件
             在包含文件关系中：可以用通配符“*”(表示任意字符)
             注意：主配置文件命名不包含通配符的范围内



    -->
    <!--
    <import resource="classpath:ba04/Spring-School.xml" />
    <import resource="classpath:ba04/Spring-Student.xml"/>
    -->
    <import resource="classpath:ba04/Spring-*.xml" />
</beans>